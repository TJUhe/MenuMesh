# 外部机器 MinGW Ninja Release 构建错误诊断

## 原始现象

另一台 Windows 机器运行 VS Code 任务中的 `build: mingw+ninja release all` 时，编译和链接基本完成，但在生成 `bin/line_quadrics_qem_tests.exe` 后失败：

```text
CMake Error at .../GoogleTestAddTests.cmake:77 (message):
  Error running test executable.
  Path: build/mingw-ninja-release/bin/line_quadrics_qem_tests.exe
  Result: Exit code 0xc0000139
```

`0xc0000139` 表示测试可执行文件启动时动态库符号解析失败，常见原因是 Windows 从 `PATH` 中加载到了错误版本的 MinGW 运行时 DLL，或测试 exe 所在目录缺少当前编译器对应的 `libstdc++-6.dll`、`libgcc_s_*.dll`、`libwinpthread-1.dll`。

## 当前修复

已在 `b81a45d` 中修复并推送：

- CMake 会根据 `CMAKE_CXX_COMPILER` 所在目录收集 MinGW 运行时 DLL，并通过全局目标 `lq_mingw_runtime_dependencies` 复制到构建目录 `bin`。
- GoogleTest 的 `gtest_discover_tests` 改为 `DISCOVERY_MODE PRE_TEST`，避免 Release 全量构建阶段因为测试发现提前启动 exe 而失败。
- `docs/guide/vscode_setup.md` 已补充中文排查步骤。

## 当前验证命令

```powershell
cmake -S . -B build/mingw-ninja-release -G Ninja `
  -DCMAKE_BUILD_TYPE=Release `
  -DCMAKE_C_COMPILER=gcc `
  -DCMAKE_CXX_COMPILER=g++ `
  -DCMAKE_EXPORT_COMPILE_COMMANDS=ON `
  -DLQ_BUILD_PERFORMANCE_TESTS=OFF
cmake --build build/mingw-ninja-release --parallel
ctest --test-dir build/mingw-ninja-release -LE performance --output-on-failure
```

本机验证结果：Release 全量构建通过，非性能测试 72/72 通过。

## 如果另一台机器仍失败

先检查工具链是否混用：

```powershell
where g++
where gcc
where cmake
where ninja
Get-ChildItem build\mingw-ninja-release\bin\*.dll
```

如果 `where g++` 和实际希望使用的 MinGW 不一致，先修正 `PATH`，删除或重新配置 `build/mingw-ninja-release`，再重新构建。
