# Common 3D Test Models

OBJ fixtures used by external-model validation.

These files are kept under `tests/data/external/` because they are validation
inputs, not example assets. CLI validation output generated from these files
belongs under `tests/output/external_model_validation/`.
