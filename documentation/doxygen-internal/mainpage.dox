/**
 * @mainpage ManuMesh 内部源码参考
 *
 * @section internal_scope 范围
 *
 * 本站点是 ManuMesh 实现代码的内部工程参考。它包含 `include/` 下的公共声明以提供上下文，
 * 以及 `src/` 下的全部实现文件，包括私有、静态、局部、匿名命名空间和其他未文档化的符号。
 *
 * @section internal_navigation 导航
 *
 * - 使用 **Files** 按源码目录浏览实现。
 * - 使用 **Namespaces**、**Classes** 和 **Class Members** 查找符号。
 * - 打开文件的 **Source** 视图查看带交叉引用的内联代码。
 * - Graphviz 可用时，函数页面会包含调用者和被调用者图。
 *
 * @section internal_build 构建
 *
 * @code{.powershell}
 * cmake --build build/mingw-ninja-release --target check-src-doxygen
 * cmake --build build/mingw-ninja-release --target docs-internal --parallel
 * @endcode
 *
 * 生成的入口是 `docs/internal/html/index.html`。
 *
 * @section internal_policy 文档策略
 *
 * 内部文档使用 `EXTRACT_ALL=YES` 生成，因此仍可浏览局部 helper 实现。每个 `src/` 文件都有块式
 * `@file`/`@brief`/`@ingroup` 头；内部类型和头文件声明的接口使用显式 `/** ... */` 文档块。
 * `check-src-doxygen` 目标强制执行该格式，并拒绝行式 Doxygen 注释。普通 `//` 仅用于局部证明义务、
 * 数值推导和不明显的实现选择。
 *
 * @warning 这是内部实现视图，不是已安装 SDK 的契约；它可能在没有 API 或 ABI 兼容性保证的情况下变化。
 */
